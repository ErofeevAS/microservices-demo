spring:
  application:
    name: twitter-to-kafka-servie
  profiles:
    active: twitter_to_kafka
  config:
    import: 'configserver:'
  cloud:
    config:
      name: twitter-to-kafka-servie,config-client
      uri: http://localhost:8888